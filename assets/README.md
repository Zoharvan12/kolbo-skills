# Assets

Branding assets referenced from `.codex-plugin/plugin.json` (`composerIcon`, `logo`).

| File | Used by |
|---|---|
| `icon.svg` | Codex composer icon — small vector |
| `logo.png` | Codex plugin card — larger raster (recommended 512×512) |

To update: replace the file in place and bump the repo `VERSION`. The Codex plugin loader picks up the asset on next install.
